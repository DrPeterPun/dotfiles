# funcao de abrir cenas com o default
open(){
    for i
    do xdg-open $i &
    done
}

#reload zsh
alias szsh="source ~/.zshrc"

#alias do vim
alias vm="nvim"
alias vim="nvim"
alias nvm="nvim"

# alias de nova comand line
alias cmd="alacritty &"

alias qq="exit"

alias pyenv="source bin/activate"
#
# adicionar o latex ao path
PATH="/usr/local/texlive/2023/bin/x86_64-linux:$PATH"

# alias git 
alias ga="git add"
alias gaa="git add ."
alias gp="git push"

# git commit
gcm() {
    # Get all arguments after the command keyword
    local args="${@#gcm }"
    # Print the arguments
    git commit -m "$args"
}

# Alias the function to a shorter command name if desired
